1.0.7 (fix / improvement)
- Fixed sync never running when "Do not run on low battery" or "Only while charging" was enabled
- The execution log is now colour-coded (sync start, upload, download, delete and errors get their own colour and marker, plus date separators)
